import { Routes } from '@angular/router';
import { MainContentComponent } from './dashboard/dashboard.component';

export const routes: Routes = [
    { path: '', component: MainContentComponent },
];
